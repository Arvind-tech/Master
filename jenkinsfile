pipeline {
       agent speedy
             stages{
                  stage('maven version'){
                  agent {docker 'jenkins/slave'}
                     steps{
                        script{
                          sh "mvn --version"
                          }
                         }
                   }
}
}
