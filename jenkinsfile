pipeline {
       agent {docker 'jenkins/slave'}
             stages{
                  stage('maven version'){                  
                     steps{
                        script{
                          sh "mvn --version"
                          }
                         }
                   }
}
}
