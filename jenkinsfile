pipeline {
       agent none
             stages{
                  stage('maven version'){
                  agent {docker 'maven:3-alpine'}
                     steps{
                        script{
                          sh "mvn --version"
                          }
                         }
                   }
}
}
